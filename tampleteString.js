// const message =`hi, ${person.name} has a: ${a} access to ${number[2]}`
// if(true){

// }


// multiply the arrow function 
const square= x => x * x;
const result = square(10);
console.log(result);
